<!DOCTYPE html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7" lang="en"><![endif]-->
<!--[if (IE 7)&!(IEMobile)]><html class="no-js lt-ie9 lt-ie8" lang="en"><![endif]-->
<!--[if (IE 8)&!(IEMobile)]><html class="no-js lt-ie9" lang="en"><![endif]-->
<!--[if gt IE 8]><!--><html class="no-js no-touch" lang="en"><!--<![endif]-->
  <head>
    <meta charset="utf-8">
    <title>issue</title>

    <!-- http://t.co/dKP3o1e -->
    <meta name="HandheldFriendly" content="True">
    <meta name="MobileOptimized" content="320">
    <meta name="viewport" content="width=device-width,minimum-scale=1.0,maximum-scale=1.0" />
    <link rel="shortcut icon" href="/favicon.ico">

    <!--iOS -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
    <link rel="stylesheet" href="/assets/application.css" type="text/css">
    
    <!-- Web UI -->
    <link rel="stylesheet" href="/assets/app/ui.css" type="text/css">

    <script src="/assets/issue.js"></script>
    <script src="/assets/app/models.js"></script>
    <script src="/assets/app/magazine.js"></script>
    
    <script>
      App.detectLayout()
    </script>
    
    <% if defined?(current_issue) %>
      <% current_issue.assets.to_a.each do |asset| %>
        <% if asset.match(/\.js$/) %>
          <script src="<%= "/#{params[:issue]}/assets/#{asset}" %>"></script>
        <% elsif asset.match(/\.css$/) %>
          <link rel="stylesheet" href="<%= "/#{params[:issue]}/assets/#{asset}" %>" type="text/css">
        <% end %>
      <% end %>
    <% end %>
  </head>

  <body class="<%= @body_class %>">
    <div role=main class="fashion-theme content-view">
      <%= yield %>
    </div>
  </body>
  
  <script>
    App.init()
  </script>
  
  <% if respond_to?(:yield_content) %>
    <%= yield_content :js %>
  <% else %>
    <%= yield :js %>
  <% end %>
  
</html>
