class PageView extends Backbone.View
  
  initialize: ->
    @hotspot = new Hotspot(el: ".hotspot")
    
  render: ->
    
  next: ->
  
  prev: ->
    
  back: ->
  
  
# Export to global
this.PageView = PageView