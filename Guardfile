guard 'pow' do
  watch('.powrc')
  watch('.powenv')
  watch('.rvmrc')
  watch('config.ru')
  watch('Gemfile')
  watch('Gemfile.lock')
  watch('preview.rb')
  # watch(%r{^config/environments/.*\.rb$})
  # watch(%r{^config/initializers/.*\.rb$})
end
